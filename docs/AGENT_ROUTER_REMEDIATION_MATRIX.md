# Agent-router remediation requirement/evidence matrix

Status: active remediation ledger. The code in this worktree is based on the
merged PR #2 commit `af74d10` (2026-09-14). This file deliberately separates
deterministic component evidence from live Linear/GitHub/Codex proof.

## Frozen base evidence

| Evidence | Result |
| --- | --- |
| `origin/main` after fetch | `af74d10f9aa799476eabfc5c856c956c03b88c4e` |
| PR #2 | merged; merge commit `af74d10`; no merge was performed by this task |
| clean baseline command | `mise exec -- mix test --seed 0` from `elixir` |
| clean baseline result | `351 tests, 0 failures, 6 skipped` |
| prior characterization probe | user-supplied `docs/router_review_probe_test.exs`; 8 tests, 0 failures, seven tests characterize unsafe behavior |
| live resources | no disposable Linear project, GitHub repository, or real Codex session has been authorized/provided |

## Review findings

The implementation and evidence columns are updated as each bounded task is
completed. “Pending” is intentional until a test and validation command exist.

| ID | Requirement / risk | Regression test | Implementation evidence | Commit evidence | Validation evidence | Live-proof status / limitation |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | Effective responsibility must authorize runtime, sandbox, command, and prompt; normalized-name and override bypasses must fail closed; invalid reload keeps LKG. | `agent_router_test.exs`: contradictory capability, standard-role identity, collision, and LKG reload tests | `Profile.validate_effective_policy/1`; `Router.validate_routes/2`; schema rejects contradictory routed profiles | `df419fb` | Affected 89-test suite and full `make all` passed | Component proof only until disposable resources exist |
| R2 | Absent profiles must preserve legacy read-only behavior; writable routed defaults require explicit migration. | `agent_router_test.exs` and `profile_runtime_test.exs`: legacy mode retains nil profiles and configured read-only policy; orchestrator legacy dispatch test | `Schema.Agent.routing` defaults to `legacy`; routed profiles require explicit `routing: routed`; legacy route does not apply profile sandbox | `df419fb` | Affected 89-test suite and full `make all` passed | Component proof only; real Codex denied-write proof unavailable |
| R3 | Poll refresh must stop a running worker when a blocker/route/state becomes unsafe; completion and shutdown races are idempotent. | `agent_router_orchestrator_test.exs`: task-supervised long-running worker stopped on blocker reappearance; route-change worker stop/retry; stale DOWN/dependency messages are ignored | Poll reconciliation fetches fresh issue and full dependency graph state, applies the dependency guard to active attempts, records blocked attempt metadata, and schedules route-change retries while retaining claims | `5884b35` | Affected 134 tests, 0 failures; `mise exec -- make all`: 376 tests, 0 failures, 6 skipped; 100% total coverage; Credo clean; Dialyzer 0 errors | Component proof only; real Codex/Linear resources unavailable |
| R4 | Linear relations require complete pagination and explicit missing/malformed/error state; empty must be verified empty. | `dependency_completeness_test.exs`: 55-relation pagination, mixed relation types, explicit empty, missing/malformed page-info, missing blocker state, and provider-error tests | `Linear.Client` paginates every inverse-relation connection, preserves only `blocks`, and marks missing/malformed relation metadata incomplete; `Tracker.Issue` carries completeness | `30e0edb` | `mise exec -- make all`: 374 tests, 0 failures, 6 skipped; 100% total coverage; Credo clean; Dialyzer 0 errors | Provider-shaped component proof only; disposable Linear proof unavailable |
| R5 | Effective profile model/sandbox/command must reach every runtime turn. | `profile_runtime_test.exs` and `agent_runtime_test.exs`: startup plus every turn, including a two-turn continuation; `core_test.exs`: traced `turn/start` model payload | `AgentRunner` stores normalized `runtime_opts` in the session context and reuses them for turn calls | `df8db00` | `mise exec -- make all`: 362 tests, 0 failures, 6 skipped; 100% coverage; Credo clean; Dialyzer 0 errors | Component proof only; real Codex process unavailable |
| R6 | Graph acquisition must include non-dispatchable closure nodes and detect self/multi-node cycles without blocking independent work. | `dependency_completeness_test.exs`: all-state full-graph pagination, inactive closure cycle, and final refresh race; `dependency_graph_test.exs`: self-cycle/independent component and completeness diagnostics | `Tracker.fetch_dependency_graph/0`, Memory/Linear full-graph adapters, `Graph.completeness`, and orchestrator poll/final graph refreshes | `30e0edb` | `mise exec -- make all`: `374 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Component/provider-shaped proof only; live provider graph still unverified |
| R7 | Role prompts must be coherent, runtime-reloadable for future attempts, stable for active attempts, and packaged. | `role_prompt_test.exs`: runtime file reload/fallback, active-attempt stability across two turns, shipped role permissions, and workflow coherence | `PromptBuilder` reads the runtime prompt root first and falls back to embedded prompts; `AgentRunner` captures the role prompt at attempt start; planner/builder/reviewer/fixer prompts declare bounded permissions | `3ac2109` | Affected 144 tests, 0 failures; `mise exec -- make all`: `380 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Component proof only; real Codex process unavailable |
| R8 | Bound ordinary retries and review cycles; separate capacity/continuation/route-change accounting; CI retry disabled or tightly bounded. | `attempt_policy_test.exs` and `core_test.exs`: ordinary failure 1/2/3 then human attention, reviewer→correction cycle cap, capacity wait, terminal reset, nonterminal preservation, and reload retention | `AgentRuntime.AttemptPolicy` owns independent counters and a disabled CI policy; `Orchestrator` records failures, review cycles, capacity waits, continuations, and route changes, claims retry entries, blocks exhausted work, and resets only on terminal completion | `3f8d6f1` | Affected policy/orchestrator/core suite: `69 tests, 0 failures`; `mise exec -- make all`: `389 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Component proof only; live Linear/GitHub/Codex retry behavior unavailable |
| R9 | Snapshot/API must retain truthful termination reasons, sandbox, attempts, dependency completeness, and safe redaction. | `observability_contract_test.exs` plus `orchestrator_status_test.exs`, `extensions_test.exs`, and `status_dashboard_snapshot_test.exs` | Orchestrator snapshots and bounded recent-attempt history now expose effective sandbox/profile/responsibility, session/workspace IDs, counters, dependency completeness, and classified reasons for normal completion, route changes, dependency stops, capacity waits, runtime failures/unavailability/stalls, shutdown, terminal cleanup, and retry exhaustion. Presenter/API and terminal/LiveView dashboards consume allowlisted redacted projections for errors, Codex events, routes, dependencies, graph diagnostics, and rate limits. | `d45d8c3` | Focused contract: `9 tests, 0 failures`; affected set: `138 tests, 0 failures`; `mise exec -- make all`: `398 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors; `git diff --check` clean | Component proof only; no live Codex/Linear/GitHub resources authorized |
| R10 | Merge gate must remain non-executable; fake proof must not be presented as live Codex/Linear/GitHub proof. | `agent_router_test.exs`: merge Codex/workspace-write/command override rejected; `live_proof_gate_test.exs`: missing consent/config skips live proof | Merge responsibility is fixed to deferred/read-only/no command in profile validation; routed schema rejects executable override; the live-proof harness requires exact consent, named disposable resources, credentials, and an explicit Codex home | `df419fb`, `8a28391`, `d482e36` | `mise exec -- make all`: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors; focused live/gate set: `11 tests, 0 failures, 6 skipped` | Live proof remains blocked pending explicit disposable resources and authorization; skipped tests are not live evidence |

## Additional gaps from the review

| Gap | Requirement | Regression test | Implementation evidence | Commit evidence | Validation evidence | Limitation |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | Preserve a single authority per concern and do not add Redis/Postgres/Ash/second scheduler. | Architecture/doc review plus full suite | The remediation diff preserves the existing orchestrator as scheduler/authority; no Redis, Postgres, Ash, second scheduler, Cursor, automatic merge, or merge executor was introduced. | `af74d10..d482e36` | `mise exec -- make all`: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Live deployment topology not exercised |
| A2 | Enforce workflow-controlled tracker transitions at the existing agent-tool boundary, including unresolved `In Review` → `Ready to Merge`. | `transition_policy_test.exs`: role-owned handoffs, planner/reviewer/merge denial, dependency denial, and fail-closed context; `dynamic_tool_test.exs`: raw mutation denial, builder/fixer handoffs, unresolved/incomplete denial, state-ID verification, and safe errors | `Tracker.TransitionPolicy` authorizes only responsibility-owned handoffs; `AgentRunner` captures issue state, route responsibility, and dependency decision; `AppServer`/`DynamicTool`/`Tracker` bind that context immutably; Linear exposes read-only `linear_graphql` plus verified `linear_transition` using a fixed mutation | `8a28391` | Focused policy/tool set: `31 tests, 0 failures`; affected runtime/tool/app-server/provider set: `75 tests, 0 failures`; full run: `413 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors; `git diff --check` clean | Component/provider-shaped proof only. The controlled Linear boundary rejects raw Linear GraphQL mutations; other adapter-wide APIs and external provider-native clients remain outside this policy. No live proof |
| A3 | Make custom profiles selectable only through explicit responsibility-aware routes. | `agent_router_test.exs`: custom profile route selection, responsibility mismatch, and schema reference tests | `Router.resolve/3` and `Router.validate_routes/2`; unreferenced custom profiles rejected in routed schema | `df419fb` | Affected 89-test suite passed; final `mise exec -- make all`: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Component proof only |
| A4 | Use concurrency class/capacity semantics or remove misleading configuration. | `core_test.exs`: global/per-state/per-host capacity pressure, host caps, and state limits; `agent_router_test.exs`/`profile_runtime_test.exs`: routed profile field retention | Global, per-state, and per-host caps are enforced by the existing orchestrator; `concurrency_class` is retained for route identity and observability only, not treated as a separate capacity pool. | `df419fb`, `3f8d6f1`, `d482e36` | `mise exec -- make all`: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Real multi-host capacity is not available |
| A5 | Keep opaque runtime/session errors and workspace cleanup safe while redacting public payloads. | Existing runtime/cleanup tests plus `observability_contract_test.exs` redaction cases | Public snapshots/API/dashboard payloads now bound error text and allowlist provider-shaped event/route/graph/rate-limit data; opaque runtime details remain internal. Workspace cleanup behavior remains covered by the existing orchestrator tests. | `d45d8c3` | Task 7 focused/affected suites and `mise exec -- make all`: `398 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors | Real Codex process unavailable; live provider error shapes not exercised |
| A6 | Update root README, Elixir README, and workflow documentation when behavior/config changes. | Docs review covers retry/review/CI policy, capacity semantics, routed lifecycle, and live-proof requirements in root `README.md`, `elixir/README.md`, `elixir/WORKFLOW.md`, and `SPEC.md` | Retry limits, capacity accounting, disabled CI retry, reload/restart boundary, `concurrency_class` semantics, and the consent-gated live-proof procedure are documented alongside routed lifecycle behavior. | `d482e36` | `mise exec -- make all`: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors; `git diff --check` clean | Docs cannot substitute for live operational proof |
| A7 | Add opt-in live-proof harness with named disposable resources and a hard consent gate. | `live_proof_gate_test.exs`: five no-consent/missing-config refusal cases; all live provider modules use the gate | `SymphonyElixir.LiveProofGate` requires the exact consent token, provider-specific run flag, named disposable scope, credentials, and explicit `SYMPHONY_LIVE_CODEX_HOME`; it never prints secret values and never falls back to `CODEX_HOME`/`~/.codex`. | `d482e36` | Focused live/gate set: `11 tests, 0 failures, 6 skipped`; full run: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors; `git diff --check` clean | No disposable resources or authorization were supplied; no live claim is made and skipped tests are not evidence |

## SYM micro-prompts

| ID | Required evidence | Current status at merged base | Commit evidence | Final evidence |
| --- | --- | --- | --- | --- |
| SYM-00 | Freeze base and avoid merging an unverified PR. | Historical blocker cleared: PR #2 is now merged at `af74d10`; work is isolated on `remediation/agent-router`. | `af74d10` | Base and current worktree were verified; the branch remains isolated and no merge or push was performed by this task. |
| SYM-01 | Planner reads and produces a bounded plan without source writes. | Existing lifecycle proof plus `role_prompt_test.exs` role-permission assertions | `3ac2109` | Shipped planner prompt declares planning responsibility, read-only runtime permission, bounded planning output, and no source/approval/merge authority. Full gate: `418 tests, 0 failures, 6 skipped`. |
| SYM-02 | Reviewer is read-only and reports exact PASS/FAIL/BLOCKED evidence. | Existing lifecycle proof plus `role_prompt_test.exs` reviewer-permission assertions | `3ac2109` | Shipped reviewer prompt declares read-only permission and exact PASS/FAIL/BLOCKED output with no source, approval, or merge authority. Full gate: `418 tests, 0 failures, 6 skipped`. |
| SYM-03 | Ready/In Progress implementation dispatch obeys dependencies and profile policy. | Existing route tests plus new legacy/profile-policy regressions | `df419fb` | Explicit routed mode and effective responsibility/sandbox validation. Final gate: `418 tests, 0 failures, 6 skipped`. |
| SYM-04 | Changes Requested correction is bounded, reviewed, and retry-limited. | `core_test.exs` simulates four reviewer→correction route boundaries with real orchestrator state and asserts the fourth is blocked; `transition_policy_test.exs` and `dynamic_tool_test.exs` deny correction unless dependencies are complete and satisfied | `3f8d6f1`, `8a28391` | `AttemptPolicy` caps review cycles at three; route changes remain separate; `Tracker.TransitionPolicy` limits correction to the fixer and requires a complete, allowed dependency decision; exhausted work is retained as human-attention blocked state. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. | Component proof only; live provider transitions unavailable |
| SYM-05 | Profile runtime options survive into each turn. | Characterization reproduced nil model; new continuation and `turn/start` tests now assert selected model | `df8db00` | Effective options are captured per attempt and passed to every turn. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-06 | Dependency policy distinguishes Done, active, and invalidated blockers. | Existing policy tests pass; completeness/closure was insufficient at base. | `30e0edb` | `dependency_completeness_test.exs` covers incomplete metadata, closure, and missing/malformed blocker data. `Dependency.Guard` allows read-only planning/review on incomplete data and denies implementation/correction/merge; completeness propagates through Linear, Memory, Tracker, and orchestrator refreshes. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-07 | Blocker reappearance/reconciliation stops unsafe work. | Existing turn-boundary stop test plus task-supervised poll test in `agent_router_orchestrator_test.exs` | `5884b35` | Fresh running-issue graph reconciliation stops implementation workers and retains a dependency-blocked claim with attempt metadata. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-08 | Merge route is deferred/non-executable. | Merge override regression in `agent_router_test.exs`; `transition_policy_test.exs` denies planner/reviewer/merge lifecycle authority and requires `merge_permitted`; `dynamic_tool_test.exs` rejects raw lifecycle mutations and unsafe merge transitions | `df419fb`, `8a28391` | Merge tuple cannot be made executable through config; `Tracker.TransitionPolicy` permits only a verified review→Ready to Merge transition when dependencies are complete and the explicit merge gate is true, while merge responsibility remains non-executable. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. | Component/provider-shaped proof only; live provider transitions and merge gate integration unavailable |
| SYM-09 | Relation pagination/completeness is authoritative. | Characterization exposed missing relation data becoming `[]`; current query capped at 50. | `30e0edb` | `dependency_completeness_test.exs` proves pagination, empty verification, malformed metadata, missing state, and errors. Linear relation connections carry pageInfo, paginate to completion, and return explicit incomplete/error results. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-10 | Full project graph catches cycles outside active candidates. | Characterization exposed active-only graph omission. | `30e0edb` | New tests cover inactive closure and final refresh races. Poll and final dispatch paths fetch the adapter’s all-project dependency graph; Memory and Linear include non-dispatchable issues. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-11 | Independent graph components continue while cyclic components stop. | Existing partial component proof; full graph acquisition was absent. | `30e0edb` | `dependency_graph_test.exs` covers self-cycle/independent components; `dependency_completeness_test.exs` covers inactive-cycle acquisition. `Dependency.Graph` retains component-local cycle detection while graph completeness/diagnostics fail closed only where dependency data is unsafe. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-12 | Poll/completion/shutdown races do not resurrect workers. | `agent_router_orchestrator_test.exs` sends stale DOWN and dependency messages after poll termination; route-change retry claim remains stable | `5884b35` | Task termination demonitor/flushes the worker reference; stopped attempts are absent from `running`, and route changes retain a single retry claim. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. |
| SYM-13 | Prompt reload and shipped workflow are coherent. | `role_prompt_test.exs` proves runtime reload, packaged fallback, active-attempt stability, role coherence, and routed `WORKFLOW.md` parsing | `3ac2109` | Prompt files are runtime-first with embedded fallback; shipped workflow opts into routed profiles and removes the conflicting legacy lifecycle body. Final gate: `418 tests, 0 failures, 6 skipped`. |
| SYM-14 | Retry/review/CI limits are explicit and enforced. | `attempt_policy_test.exs` and `core_test.exs` assert three-retry/three-cycle thresholds, capacity separation, terminal/reload behavior, and CI disabled | `3f8d6f1` | `AgentRuntime.AttemptPolicy` exposes the three-retry/three-cycle limits and `ci_retry_policy/0 == :disabled`; orchestrator emits human-attention blocked records at threshold. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. | Live proof blocked; no disposable provider resources authorized |
| SYM-15 | Observability accurately distinguishes dependency, capacity, retry, and termination states. | `observability_contract_test.exs`: allowed dependency observation, dependency stop, capacity wait, normal completion, terminal cleanup, retry exhaustion, and provider/event/error redaction | `d45d8c3` | Presenter reports `observed` unless dependency policy explicitly denies work; snapshots retain bounded recent history and classify capacity wait, route change, terminal, shutdown, runtime unavailable/stalled/failure, dependency stop, and retry exhaustion separately. Sandbox, profile/responsibility, counters, session/workspace IDs, and completeness are propagated through the shared API/dashboard projections. Final gate: `418 tests, 0 failures, 6 skipped`; 100% total coverage; Credo clean; Dialyzer 0 errors. | Component proof only; no live provider/runtime evidence |

## Validation ledger

Each completed task must add its focused command/result here and retain the
full-suite result in the handoff. Until then, no row above is a completion
claim.

| Task | Focused/affected result | `mise exec -- make all` | `git diff --check` | Commit |
| --- | --- | --- | --- | --- |
| Baseline | `351 tests, 0 failures, 6 skipped` | Pending after remediation | Clean at base | `af74d10` |
| Task 1 | 361 full tests, 0 failures, 6 skipped; 100% coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `df419fb` |
| Task 2 | `362 tests, 0 failures, 6 skipped`; 100% coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `df8db00` |
| Task 3 | `dependency_completeness_test.exs` plus affected graph/policy/adapter/orchestrator suite; full run: `374 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `30e0edb` |
| Task 4 | Affected reconciliation/runtime suite: `134 tests, 0 failures`; full run: `376 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `5884b35` |
| Task 5 | Affected prompt/runtime/config suite: `144 tests, 0 failures`; full run: `380 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `3ac2109` |
| Task 6 | Affected policy/orchestrator/core suite: `69 tests, 0 failures`; full run: `389 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `3f8d6f1` |
| Task 7 | `observability_contract_test.exs`: `9 tests, 0 failures`; affected status/presenter/API/dashboard/orchestrator/core set: `138 tests, 0 failures`; full run: `398 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `d45d8c3` |
| Task 8 | `transition_policy_test.exs` + `dynamic_tool_test.exs`: `31 tests, 0 failures`; affected runtime/tool/app-server/provider set: `75 tests, 0 failures`; full run: `413 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `8a28391` |
| Task 9 | `live_proof_gate_test.exs` + live provider set: `11 tests, 0 failures, 6 skipped`; full run: `418 tests, 0 failures, 6 skipped`; 100% total coverage | `mise exec -- make all`: passed; Credo clean; Dialyzer 0 errors | Clean | `d482e36` |
| Task 10 | Final fresh verification and matrix audit | Pending | Pending | Pending |

## External-proof boundary

No live claim is made in this ledger. A live proof run may be added only after
the user identifies disposable resources and explicitly authorizes the run.
The local evidence will remain honest about fake runtimes, in-memory trackers,
and provider-shaped fixtures. The implementation introduces no Cursor path,
automatic merge, Redis, Postgres, Ash, or second scheduler.
